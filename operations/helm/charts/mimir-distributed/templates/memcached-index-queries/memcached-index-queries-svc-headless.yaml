{{- if (index .Values "memcached-index-queries").enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimir.resourceName" (dict "ctx" . "component" "memcached-index-queries") }}-headless
  labels:
    {{- include "mimir.labels" (dict "ctx" . "component" "memcached-index-queries") | nindent 4 }}
    prometheus.io/service-monitor: "false"
    {{- with (index .Values "memcached-index-queries").service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- toYaml (index .Values "memcached-index-queries").service.annotations | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: memcached-client
      port: {{ (index .Values "memcached-index-queries").port }}
      targetPort: {{ (index .Values "memcached-index-queries").port }}
    {{ if (index .Values "memcached-index-queries").metrics.enabled }}
    - name: exporter-http-metrics
      port: {{ (index .Values "memcached-index-queries").metrics.containerPort }}
      targetPort: {{ (index .Values "memcached-index-queries").metrics.containerPort }}
    {{ end }}
  selector:
    {{- include "mimir.selectorLabels" (dict "ctx" . "component" "memcached-index-queries") | nindent 4 }}
{{- end -}}
