{{- if (index .Values "memcached-results").enabled -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "mimir.resourceName" (dict "ctx" . "component" "memcached-results") }}
  labels:
    {{- include "mimir.labels" (dict "ctx" . "component" "memcached-results") | nindent 4 }}
  annotations:
    {{- toYaml (index .Values "memcached-results").annotations | nindent 4 }}
spec:
  podManagementPolicy: {{ (index .Values "memcached-results").podManagementPolicy }}
  replicas: {{ (index .Values "memcached-results").replicas }}
  selector:
    matchLabels:
      {{- include "mimir.selectorLabels" (dict "ctx" . "component" "memcached-results") | nindent 6 }}
  updateStrategy:
    {{- toYaml (index .Values "memcached-results").statefulStrategy | nindent 4 }}
  serviceName: {{ template "mimir.fullname" . }}-memcached-results

  template:
    metadata:
      labels:
        {{- include "mimir.podLabels" (dict "ctx" . "component" "memcached-results") | nindent 8 }}
        {{- with (index .Values "memcached-results").podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with (index .Values "memcached-results").podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

    spec:
      serviceAccountName: {{ template "mimir.serviceAccountName" . }}
      {{- if (index .Values "memcached-results").priorityClassName }}
      priorityClassName: {{ (index .Values "memcached-results").priorityClassName }}
      {{- end }}
      securityContext:
        {{- toYaml (index .Values "memcached-results").securityContext | nindent 8 }}
      initContainers:
        {{- toYaml (index .Values "memcached-results").initContainers | nindent 8 }}
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
      {{- range .Values.image.pullSecrets }}
        - name: {{ . }}
      {{- end }}
      {{- end }}
      nodeSelector:
        {{- toYaml (index .Values "memcached-results").nodeSelector | nindent 8 }}
      affinity:
        {{- toYaml (index .Values "memcached-results").affinity | nindent 8 }}
      tolerations:
        {{- toYaml (index .Values "memcached-results").tolerations | nindent 8 }}
      terminationGracePeriodSeconds: {{ (index .Values "memcached-results").terminationGracePeriodSeconds }}
      containers:
        {{- if (index .Values "memcached-results").extraContainers }}
        {{ toYaml (index .Values "memcached-results").extraContainers | nindent 8 }}
        {{- end }}
        - name: memcached
          image: {{ (index .Values "memcached-results").image.repository}}:{{ (index .Values "memcached-results").image.tag}}
          imagePullPolicy: {{ (index .Values "memcached-results").image.pullPolicy }}
          ports:
            - containerPort: {{ (index .Values "memcached-results").port }}
              name: client
          args:
            - -m {{ (index .Values "memcached-results").allocatedMemory }}
            - -o
            - modern
            - -I {{ (index .Values "memcached-results").maxItemMemory }}m
            - -c 16384
            - -v
            - -u {{ (index .Values "memcached-results").port }}
            {{- range $key, $value := (index .Values "memcached-results").extraArgs }}
            - "-{{ $key }} {{ $value }}"
            {{- end }}

      {{- if (index .Values "memcached-results").metrics.enabled }}
        - name: exporter
          image: {{ (index .Values "memcached-results").metrics.image.repository}}:{{ (index .Values "memcached-results").metrics.image.tag }}
          imagePullPolicy: {{ (index .Values "memcached-results").metrics.image.pullPolicy }}
          ports:
            - containerPort: {{ (index .Values "memcached-results").metrics.containerPort }}
              name: http-metrics
          args:
            {{- range $value := (index .Values "memcached-results").metrics.args }}
            - {{ $value }}
            {{- end }}
      {{- end }}
{{- end -}}
