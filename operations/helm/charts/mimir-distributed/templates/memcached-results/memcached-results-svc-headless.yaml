{{- if (index .Values "memcached-results").enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimir.resourceName" (dict "ctx" . "component" "memcached-results") }}-headless
  labels:
    {{- include "mimir.labels" (dict "ctx" . "component" "memcached-results") | nindent 4 }}
    prometheus.io/service-monitor: "false"
    {{- with (index .Values "memcached-results").service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- toYaml (index .Values "memcached-results").service.annotations | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: memcached-client
      port: {{ (index .Values "memcached-results").port }}
      targetPort: {{ (index .Values "memcached-results").port }}
    {{ if (index .Values "memcached-results").metrics.enabled }}
    - name: exporter-http-metrics
      port: {{ (index .Values "memcached-results").metrics.containerPort }}
      targetPort: {{ (index .Values "memcached-results").metrics.containerPort }}
    {{ end }}
  selector:
    {{- include "mimir.selectorLabels" (dict "ctx" . "component" "memcached-results") | nindent 4 }}
{{- end -}}
